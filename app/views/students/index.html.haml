%h1 Student Records

%table.u-full-width
  %thead
    %tr
      %th First Name
      %th Last Name
      %th State ID
      %th Student Record
      %th Edit Record
      %th Delete Record
  %tbody
    - Student.all.each do |student|
      %tr
        %td= student.first_name
        %td= student.last_name
        %td= student.state_id
        %td= link_to 'Student Record', student_path(student)
        %td= link_to 'Edit Record', edit_student_path(student)
        %td= link_to 'Delete Record', student_path(student), method: :delete, data: {confirm: 'Are you sure?'}

.button= link_to 'Create New Student Record', new_student_path
